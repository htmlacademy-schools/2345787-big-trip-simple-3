import Options from './options.js';

export default class NoEvents extends Options {
  static EVERYTHING = 'Press the New Event button to create the first point';
  static FUTURE = 'There are currently no future events';
}
