import Options from './options.js';

export default class SortType extends Options {
  static DAY = 'day';
  static EVENT = 'event';
  static TIME = 'time';
  static PRICE = 'price';
  static OFFER = 'offer';
}
